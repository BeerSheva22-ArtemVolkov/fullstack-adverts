const buyTypeArray: string[] = ["Purchase", "Rent"];
const propertyTypeArray: string[] = ["Flat", "Double", "Villa"];
const electricCategoriesArray: string[] = ["Fridge", "TV", "PC", "Smartphone", "Drone", "AMP"];
const electricQualityArray: string[] = ["New", "Used"];

const MIN_MILAGE: number = 0;
const MAX_MILAGE: number = 0;

const MIN_YEAR: number = 1885;
const MAX_YEAR: number = new Date().getFullYear();

const MIN_PRICE: number = 1;
const MAX_PRICE: number = 10000000;

export { buyTypeArray, propertyTypeArray, electricCategoriesArray, electricQualityArray, MIN_MILAGE, MAX_MILAGE, MIN_YEAR, MAX_YEAR, MIN_PRICE, MAX_PRICE }